# HTTP client for Nominatim geocoding and the routing providers (BRouter, ORS).
requests>=2.31

# Claude API for the natural-language request parser (routes/nl.py).
anthropic>=1.0

# Loads ANTHROPIC_API_KEY (and ROUTEGEN_* settings) from a local .env file.
python-dotenv>=1.0
fitparse>=1.2  # parse ridden .fit files for ride-vs-route calibration (compare_ride.py)

# Web frontend (api.py + static/index.html).
fastapi>=0.100
uvicorn>=0.23
